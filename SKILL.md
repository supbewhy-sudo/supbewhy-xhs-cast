---
name: xhs-cast
description: "Use when planning or reviewing a Xiaohongshu carousel from a topic, draft, product, tutorial, viewpoint, case, review, or resource, especially when audience, page count, reader path, evidence, on-screen copy, or production handoff needs to be decided."
---

# supɃewhY-XHS-CAST

XHS CAST（小红书卡片策略工具）把已有材料整理成面向明确读者、阅读路径清楚、可以直接进入制作的多页卡片方案。

它首先解决“这组内容该让谁看到、先讲什么、凭什么相信、最后做什么”，再处理分页、标题、上屏文案和视觉交接。标题协调是质量检查，不是产品的首要价值。

## 使用边界

- 不虚构案例、数据、用户反馈、用户心理、使用场景、当前替代或效果承诺。
- 材料未提供数字、时间、频率、趋势或比较依据时，不编造，也不“合理推断”。
- 产品 README、功能清单和 Skill 规则可以证明“被设计成做什么”，不能独立证明“实际效果如何”。
- 不把规划描述成已经完成的制图、外部验证或小红书发布。
- Style、Theme、Recipe、颜色、字号和最终渲染由制卡阶段决定。

## 规划或改稿前读取

新建、修改或评审卡片规划时，按顺序读取：

1. [营销传播判断方法](references/marketing-communication-method.md)：来源账本、TA、核心价值、证据和阅读路径。
2. [策略到上屏文案的翻译层](references/marketing-translation-layer.md)：把已确认策略转成页面任务、标题和最终文案。

只询问本 Skill 的能力、文件结构或输出格式时，不必读取 references。

## 工作流

### 0. 先完成方向确认

每次新的正式规划或完整重写，在形成策略摘要之前都先给用户一张方向确认单。确认单包含：

- 内容类型
- 唯一传播目标
- 战略 TA 或读者任务
- 本次来源范围
- 唯一 CTA

已明确的字段显示当前判断，未明确的字段提供 2–3 个会导致不同页序或主张的选项，并允许自定义。即使材料看似完整，也先让用户选择“按此方向继续”或修改其中一项；不得由模型自行判断为“信息足够”而跳过。只有用户在当前请求中明确说“跳过确认”“不用问”或“直接生成”时，才可直接进入下一步。

交互方式保持行为一致、界面按环境降级：

- 在 Codex 中且 `visualize` 可用时，**REQUIRED SUB-SKILL: Use visualize**。用选择控件呈现方向确认单，并通过 `window.openai.sendFollowUpMessage` 把已选字段作为后续消息回传，再继续同一任务；不得把选择只保留在本地面板状态。
- 使用 `visualize` 前先读取并遵守其完整 `SKILL.md`；若当前环境不能加载该 Skill 或不能调用回传接口，不得模拟“已使用视觉交互”。
- 其他环境有原生选择控件时使用原生控件；没有时使用编号选项和一个自定义入口。
- 用户确认前只交付方向确认单，不生成完整策略摘要、页序或逐页文案。确认回传属于同一任务的下一步，不重复询问。

### 交互能力提示与插件建议

正式规划第一次需要用户选择时，先按当前运行时事实判断交互能力，不凭客户端名称猜测：

| 当前能力 | 处理方式 | 是否提示安装 |
|---|---|---|
| 宿主已有可回传的原生选择控件 | 直接使用原生控件 | 否 |
| `visualize` 可用且能调用 `window.openai.sendFollowUpMessage` | 使用 `visualize` 面板 | 否 |
| 没有原生控件，且 `visualize` 未安装或不可调用 | 首次提示安装/启用 Codex 的 `visualize` 插件，同时立即提供编号选项继续 | 是，当前任务提示一次 |
| 面板能显示但不能回传 | 明确提示回传不可用，降级为编号选项 | 不重复要求安装；除非运行时明确显示缺少 `visualize` |

安装提示只在“确实需要方向选择 + 当前宿主没有可用交互”两个条件同时成立时出现。只回答 Skill 能力、只评审已有方案、只要一个标题，或用户已明确“直接生成/跳过确认”时，不提示安装。

提示应说明：安装 `visualize` 后可获得可点击的方向选择并把选择回传当前对话；当前也可以直接回复编号，不影响本次任务。不要把安装建议写成完成任务的前置条件，不要未经用户授权自动安装，也不要声称所有 Codex 或 Claude 环境都支持同一种控件。

同一任务内已经提示过一次后，不重复提示；用户安装或启用后重新发起/继续任务时，重新按运行时事实检测。Claude、Claude Code 或其他已经提供原生选择控件的宿主不显示 Codex 插件安装提示。

交互能力必须先按当前运行时事实判断，不按产品说明或模型猜测判断：

- 只有当前 Skill/工具列表明确提供 `visualize`，且宿主允许 `window.openai.sendFollowUpMessage` 回传时，才标记为“可回传视觉交互”。否则标记为“不可用”，直接采用编号选项。
- 面板创建失败、宿主不支持回传、回传报错或超时，都视为“未确认”，立即降级到同一份编号确认单；不得因为面板显示过就生成完整方案。
- 回传消息必须包含本次选择的字段和固定确认标识，例如首行 `XHS_CAST_CONFIRM_V1 status=confirmed`，后面逐项列出内容类型、唯一目标、TA、来源范围和 CTA。重复回传只处理一次，不重复询问或重复生成方案；字段改变则视为新的确认，需要重新验收。
- 只有收到可解析的回传，或用户在文本中明确选择“按此方向继续/修改/跳过确认”，才算确认完成。仅回复“可以”“继续吧”“看着办”等模糊短语不算确认，应再次给出同一组选项；没有回传时保持等待状态，不把面板当前状态当作用户授权。

介绍 XHS CAST 2.0 自身时，交互承诺必须与本节一致：可以写“正式规划前先确认方向；用户可明确跳过”，不得写成由模型临时判断的“必要时可能提问”，也不得声称所有宿主都提供同一种视觉控件。

把 XHS CAST 2.0 自己拆成小红书卡片，仍然属于正式规划，默认先走方向确认；只有用户明确说“直接生成/跳过确认”才跳过。仅回答“这个 Skill 能做什么”而没有要求规划卡片时，才不启动确认流程。

方向确认单使用以下最小结构，不把它包装成完整策略摘要：

```text
方向确认
内容类型：{当前判断 / 选项 1 / 选项 2}
唯一目标：{当前判断 / 选项 1 / 选项 2}
战略 TA / 读者任务：{当前判断 / 选项 1 / 选项 2}
来源范围：{本次明确提供的文件、链接或正文}
唯一 CTA：{当前判断 / 选项 1 / 选项 2}

请选择：按此方向继续 / 修改某一项 / 跳过确认直接生成
```

选项必须是会实际改变页序、标题或证据处理的方向，不得用“其他”“都可以”凑数。每个选项必须同时展示：

1. 选项名称
2. 面向当前材料的白话适用场景
3. 选择后会怎样组织内容或页序
4. 用户最终会得到什么、读完能带走什么
5. 与其他选项的关键取舍（保留什么、舍弃什么或提高什么证据门槛）

禁止只显示“新闻速览”“单条深挖”“跨条目判断”等方法术语，也禁止用“更完整”“更专业”“更适合传播”等没有具体交付含义的形容词代替解释。解释必须写在视觉卡片、原生控件的辅助文本和编号降级版中，三种界面的语义保持一致。

默认选项可以预先选中，但必须附一句基于当前材料的推荐理由；推荐是规划决定，不得伪装成来源事实。用户选择后复述选中的 ID、完整解释和已确认字段，并把未选择的方向丢弃；回传不能只保留一个模糊的“已选择”。

新闻/日报方向至少按以下白话方式解释：

- **新闻速览**：按时间或原始栏目逐条讲清主体、动作、对象和状态；保留多条事件，读完知道本期发生了什么。
- **单条深挖**：只围绕用户点名的一个主体或事件展开背景、直接证据和边界；会舍弃其他无关条目，读完把一条消息讲透。
- **跨条目判断**：先列出可比条目，再提炼有边界的共同判断；证据门槛更高，不自动把不同机制或编辑总括合并成同一事实。

如果某个方向无法用当前材料说明适用场景、组织方式和最终交付，就先标记“材料不足，待确认”，不要把它做成可直接选择的选项。

### 1. 隔离当前内容源

只使用用户本次指定的材料。先确认对象、版本和来源范围；不要把旧任务、同目录其他项目或模型常识混进当前内容。

建立内部来源账本：

| 状态 | 可以怎么用 |
|---|---|
| 已提供 | 可进入策略、标题和上屏文字 |
| 已核实 | 可进入策略、标题和上屏文字，并保留来源 |
| 规划决定 | 可决定分组、页序和 CTA，不得伪装成 TA 事实 |
| 待确认 | 不得进入标题、页面主张、读者变化或上屏文字 |

触发场景、当前替代、主要疑虑和用户心理必须能定位到来源原句。完成方向确认后仍缺少这些信息时，逐字段标记“当前材料未提供；待确认”，并只用已知事实规划；不得把确认单中的规划选择改写成 TA 事实或效果证据。

“状态”与“来源层级”是两个不同字段，不能用一个字段代替另一个。来源账本至少区分：

| 来源层级 | 可以怎么用 | 不得怎么用 |
|---|---|---|
| 原始一手材料：原帖、官方文档、真实产物或原始数据 | 在明确范围、时间和口径内支持对应主张 | 不自动支持效果、普遍性或当前仍可用 |
| 条目摘要：日报对单条信息的概括 | 定位条目、组织阅读路径、提示需回查的主张 | 不代替原文证明细节、因果或语气 |
| 编辑总括：日报导语、编者趋势判断 | 作为本期内容的编辑框架或话题范围 | 不改写成行业事实、全体主体选择或能力趋势 |
| 二手转述：未核对原文的博主/媒体转述 | 只能说明“某人这样转述” | 不写成原主体声明、事实已证实或行业共识 |
| 独立评测/复现 | 在明确样本、条件和时间内支持测量或复现结果 | 不外推到所有用户、版本或长期效果 |

条目摘要或编辑总括与原始材料冲突时，保留冲突并以原始材料逐条核对；原文未提供时写“当前材料未提供；待核实”，不要用摘要补全。

### 1.5 多主体材料先做机制归类

当材料包含两个以上主体、产品、案例或观点时，先在内部建立“主体—对象—动作—机制层—来源层级—证据性质—发布状态—范围—时间”矩阵。每条信号至少记录：

- 主体与原始来源
- 对象及其具体动作
- 机制层：访问/数据边界、动作权限、交付复核、研究方向、效果结果等；只选择材料实际支持的层
- 来源层级：原始一手材料、条目摘要、编辑总括、二手转述、独立评测/复现
- 证据性质：官方或产品方说明、原始材料、个人选择/实测、独立评测、研究方向/招聘、规划决定
- 发布状态：已发布、公开 beta、小范围试用、研究中、招募中、公司自述部署、个人实测或未知/待核实；原文没有就写“材料未提供”
- 可用范围：面向谁、在哪个产品表面、组织/地区/版本和前置条件；原文没有就写“材料未提供”
- 时间：发布日期、测试窗口或当前有效期；原文没有就写“材料未提供”
- 这条材料能支持什么，以及明确不能支持什么

使用以下内部表格，空缺项写“材料未提供”，不要用模型常识补齐：

| 主体/来源 | 来源层级 | 对象与动作 | 机制层 | 发布状态 | 可用范围 | 时间 | 证据性质 | 能支持的最强结论 | 禁止升级的结论 |
|---|---|---|---|---|---|---|---|---|---|
| {谁；原文/链接} | {一手/摘要/总括/二手/独立} | {对什么做了什么} | {访问/动作/交付/研究/效果} | {已发布/beta/试用/研究等} | {人群/产品表面/地区/版本/前置条件} | {发布日期/测试窗口/有效期} | {产品方/个人/研究/独立评测等} | {原文能证明什么} | {不能推出什么} |

只有主体、对象、动作、授权关系和证据性质基本同类时，才可以使用“都”“共同”“一致”“同一机制”等集体表述。仅仅因为信号出现在同一时间窗口，或都出现“安全”“人工”“边界”等词，不得写成共同选择、共同原因或行业共识。

如果机制层不同，标题只能使用不抹平差异的上位判断，正文逐条保留差异；不要把“模型不直接访问”“限制写入动作”“先起草草案再人工复核”改写成同一个“执行前必须人工批准”。

事实语气必须同时跟随来源层级、来源性质和发布状态：产品方原文是“宣布/说明/声称/提供”，日报总括是“本期编辑归纳”，二手转述是“某人转述”，个人工作流是“选择/建议/担心”，研究或招聘是“研究/招募”，独立评测才可以写“测量/复现/发现”。研究方向、招聘岗位、公开 beta、小范围试用、公司自述部署和个人建议不得升级为普遍可用能力、行业规则或安全效果。

集体命题通过以下门槛才可上屏：机制层、动作/授权关系和证据性质必须可比；主体和对象可以不同，但任何未合并的差异都不能改变读者的判断或行动。否则使用“同一窗口出现了不同做法/信号”这类上位表达，或拆成逐条对照。`都`、`共同`、`一致`、`同一机制`、`行业开始`、`已经落地`、`更安全`、`更高效`等词默认视为高风险词，必须逐词提供来源支持；找不到就删除或降级。

### 1.7 日报、新闻和快讯先判内容模式

当来源出现日期窗口、导语、编号条目、栏目标题，或用户称它为“日报”“新闻”“快讯”“周报”时，先按新闻材料处理，不要直接套用观点类的“旧判断 → 解释缺口 → 新判断”路径。

先建立“编辑层级—事件条目”地图：

- 导语/编者总括：本期编辑框架，只能说明本期覆盖什么，不自动成为行业事实或整组主线。
- 栏目标题：编辑分类，只能帮助保持原始分组，不等于主体的共同动作。
- 条目标题与正文：谁在什么时间对什么对象做了什么，是快讯事实的最小单元。
- 条件、状态和来源注释：beta、试用、研究、招聘、个人实测、公司自述或“未提供独立评测”等限定，必须跟随对应条目。

正式分页前选择一种模式，并在策略摘要中写明：

| 模式 | 何时使用 | 页序原则 |
|---|---|---|
| 新闻速览 | 用户要介绍、播报、速览，或来源本身是日报 | 保留时间范围与原始栏目；每页播报明确主体、动作、对象和状态 |
| 单条深挖 | 用户点名一个主体、事件或问题 | 只围绕该条及其直接证据，不把同日报其他条目拼成背景事实 |
| 跨条目判断 | 用户明确要趋势、共同变化、原因或“这意味着什么”，或导语已有可核对的总括命题 | 先列出支持命题的具体条目，再标出哪些只是编辑归纳；不能仅凭同一日期、关键词或相邻栏目合并 |

默认选择新闻速览，不把“有观点”误判成跨条目判断。模式改变页序或核心主张时，必须在方向确认单中让用户选择。

在新闻模式生成页序前，先写一行“选材声明”：本组使用的日期/时间范围、原始栏目、具体条目数量、排除的条目，以及哪些句子是编辑归纳、哪些句子是条目事实。封面出现数量、主体数、时间范围或“这周/本期”等承诺时，建立逐项对账表；每个数字都要能对应后续页面中的明确条目，不能用“几种做法”“三道关卡”代替主体和事件。

新闻模式的核心交付是让读者知道“这期发生了什么”。证据性质或方法论可以作为边界页或收尾，但不能挤掉本期事件本身；如果材料不足以形成可核对的事件列表，应减少页数或先补问，不用抽象判断填充。

### 2. 形成紧凑策略摘要

只保留会改变分页、证据、标题或 CTA 的结论：

- 内容类型与唯一传播目标
- 有来源支持的 TA、任务或场景
- 产品/内容是什么，以及一个最重要的核心价值
- 关键主张及其证据状态
- 多主体材料的来源归类、机制层和各自不能支持的结论
- 集体命题门槛的结论：哪些可以合并，哪些必须分开，以及理由
- 从当前已知信息到行动的阅读路径
- 唯一 CTA
- 待确认或待补证据

不要为了填满表格而补齐字段。内部术语不进入卡片画面。

### 3. 先设计读者路径，再决定页数

每一页对应一次必要的信息变化，而不是一个功能模块或文档章节。

多主体材料先区分“共同上位判断”和“各条机制差异”：若差异会改变读者的判断或行动，就分开成对照区或独立页面；若只是同一时间窗口的并列出现，只能写“同一窗口出现了不同做法”，不能写“它们做了同一个选择”。

不要为了凑齐 3–5 页把研究方向、招聘、产品发布和个人建议平均分栏；先按读者需要的信息变化分组，再用来源性质决定每组的语气和证据提示。

默认使用 3–5 页：

| 页数 | 使用条件 |
|---|---|
| 3 页 | 一个核心答案、一个解释或证明、一个行动即可完成 |
| 4 页 | 需要把核心答案与方法、例子或边界分开 |
| 5 页 | 原理、演示或证据、风险边界都不可省，合并会过载 |

页数可以少于 3 页或多于 5 页，但每一页都必须有不可替代的传播任务。删除一页后不影响理解、相信或行动，就删除。

常见路径只作起点，不是模板：

- 产品/工具：任务或输入识别 → 它是什么与核心差异 → 如何工作 → 真实演示或证据 → 行动与边界
- 教程：任务识别 → 关键判断 → 步骤或演示 → 自查 → 行动
- 观点：已知判断 → 解释缺口 → 新判断 → 依据与边界 → 应用
- 案例：结果与条件 → 关键决定 → 过程证据 → 可迁移经验与限制
- 资源：使用任务 → 选择标准 → 资源分组 → 使用方式与边界

新闻速览另有一条最低信息路径：时间/范围与本期主题 → 条目一（主体 + 动作 + 对象/状态）→ 条目二及后续条目（保持原始栏目或明确分组）→ 读者需要记住的影响、限制或下一步。若只保留一个条目，就改用单条深挖，不要用“本期”包装单条消息。

只有来源支持的环节才可进入路径；没有真实场景或旧做法时，从对象、任务、输入或核心答案开始。

### 4. 为每页确定不同职责

先写页面任务和来源锚点，再写标题：

1. 本页只处理一个问题或决定。
2. 读者变化写成信息状态：`尚未看到/分清/拿到 X → 已经看到/分清/拿到 X`。
3. 本页主张不得高于本页证据。
4. 承接只指向下一页会提供的已知信息，不代写读者心理。

封面负责获得相关注意力，但不能只负责钩子。封面主标题必须压缩整组卡片最终要交付的核心判断、答案或价值承诺；可以用问句，但该问题必须覆盖后续主要内容，并被整组页面充分回答。不得把真正的核心结论降级成角标、装饰句或正文小字。

封面优先呈现 TA 的已知任务、输入、决定或可证明结果，不优先展示“生成标题、统一口吻、问几个问题”等内部机制。

公开页面的信息层级使用以下契约：

- 单页主标题用一条能够独立成立的事实、判断或行动标准，概括该页正文的主要信息。
- 正文解释、展开或证明标题，根据页面任务补足必要事实、原因、做法、证据或边界。
- 分类名、步骤名、数字预告、栏目名和短标签只能辅助导航或分组，不能单独充当主标题，也不能代替正文。
- 删除全部标签后，读者仍应能通过标题和正文理解该页。

### 5. 在页序确定后写标题

先收集全部页面任务，再一次性生成和检查标题。整组协调只要求：

- 对同一对象使用一致称谓。
- 叙述视角和语气不无故跳变。
- 信息密度适合整组阅读。
- 每页标题准确完成自己的页面职责。

不强制相同句式、相近字数、同一种标题模式、固定数量的数字或统一情绪强度。不同职责的页面可以使用不同标题形态。

标题逐项检查：

1. 相关：目标读者能看出与自己的任务有什么关系。
2. 可追溯：场景、比较、数字、结果和情绪都有来源。
3. 有职责：不是功能名或内部字段名，能概括本页新增信息。
4. 清楚：脱离正文仍知道对象和重点。
5. 自然：像真实创作者表达，不像模板拼接或营销术语。

新闻/日报模式再做“快讯清楚度”检查：

1. 条目页标题至少带出主体（谁/哪家公司/哪位作者）和具体动作或事件；必要时补对象、状态或时间。
2. 跨条目判断页标题必须明确是“本期归纳/这份日报记录的共同点”等编辑判断，不能伪装成每个主体都做了同一件事。
3. “依据”“为什么”“三条分别是什么”“证据性质”“应用”等只能做小标签或副标题，不能单独充当条目页主标题。
4. 封面若说“X 条”“X 层”“X 个主体”“这周”，后续页必须逐项点名并完成数量、范围和时间对账；对不上就删掉数字或改封面。
5. 只看整组主标题，读者应能复述本期至少发生了什么，而不是只复述作者的论证步骤。

写完标题后再做两级回看：

- 整组：读完全部页面后，封面标题是否准确概括了整组内容，后续页是否兑现了封面承诺？
- 单页：只看主标题，能否准确预期正文要传达的主要信息？正文是否直接解释、展开或证明标题？

### 6. 写最终上屏文字与制作交接

日常交付先给紧凑策略摘要和整组页序，再按以下契约写每页：

```markdown
## PAGE NN / 页面角色

> **页面任务｜不上屏**：{本页只完成什么}
> **读者变化｜不上屏**：尚未{信息状态 A} → 已经{信息状态 B}
> **来源锚点｜不上屏**：{材料原句、文件、链接、截图或“规划决定”}
> **事实归属/证据性质｜不上屏**：{主体、来源性质、可支持的结论与不能支持的结论}

### 上屏文字

`主标题 · H1 · PRIMARY`
{最终主标题}

`说明/主体/证据/行动 · 对应层级 · 对应主题角色`
{实际上屏文字}

### 视觉与制作交接

| 项目 | 内容 |
|---|---|
| 构图与信息密度 | {布局关系；低/中/高} |
| 第一视觉焦点 | {读者最先看到什么} |
| 阅读顺序 | {信息块顺序} |
| 重点词 | {准确引用 1–3 个上屏词组及主题角色} |
| 证据与素材 | {文件路径、截图、数据来源；没有则写待补} |
| 不进入画面 | {内部术语、未证实主张、制作说明} |
| 承接/结束动作 | {下一页已知内容或一个具体 CTA} |
| Recipe | 由制卡阶段解析 |
```

上屏文案保留完成页面任务所需的“最少但充分”信息：压缩的是重复和低价值表达，不是让标题成立所需的事实、解释、证据或边界。制作说明、推理、来源状态和内部字段留在不上屏区域。

### 7. 证据决定主张上限

不要使用“真实产物 > 官方来源 > 用户反馈 > 产品自述”的单一总排序。先识别主张类型，再匹配能直接证明它的证据：

| 主张类型 | 首选证据 | 仍需记录的限制 |
|---|---|---|
| 功能、规则或工作方式 | 实际界面/演示、官方文档、原始清单、产品方原文 | 版本、可用范围和发布时间；功能合同不等于效果 |
| 实际输出或过程 | 真实输入与完整输出、截图、可复现演示、原始产物 | 输入条件、过程范围和是否可复现 |
| 使用效果或前后变化 | 前后案例、带口径的数据、独立评测或可核实反馈 | 样本、条件、时间和因果边界 |
| 用户认可或普遍性 | 多个可核实反馈、独立调查或统计 | 不能用单个案例、点赞或产品自述外推 |
| 发布状态与可用性 | 官方发布页、版本说明、明确的 beta/试用说明和日期 | 面向谁、在哪个表面、地区/版本和有效期 |
| 风险、权限与失败边界 | 权限说明、失败条件、事故记录、独立安全评测 | 不把“有边界”写成“安全”或“能避免事故” |
| 个人选择、建议或担心 | 当事人原帖/原文 | 只能归属于当事人，不能写成行业规则 |
| 研究方向或招聘 | 研究公告、合作说明、招聘原文 | 只能说明研究/招募，不等于已发布能力 |
| 趋势、共同选择或行业判断 | 多条可比的一手材料，或有样本和口径的分析 | 同期出现、摘要总括和共享关键词不足以证明共同趋势 |

来源层级只是判断“能否回查和如何措辞”的维度，不是把所有证据排成一条强弱队列。条目摘要、编辑总括和二手转述必须回到上表对应的原始证据；回查不到时降低为“摘要/转述称”，或删除该主张。

每条公开主张至少在内部绑定：主张类型、来源层级、来源性质、发布状态、范围、时间、支持原句和不能推出的结论。产品自述和功能合同只能支持功能与边界，不能单独支持效果；没有匹配的效果证据时，把目标写成“理解、查看、验证或尝试”，不要写成“相信它已经解决”。

证据不足只允许三种处理：补问、降低或删除主张、标记待补。

## 介绍 Skill、AI 工具或开发者产品

这类内容最容易退化成内部机制说明。使用以下正向结构：

1. 封面：让读者看到自己的已知任务、输入或可证明交付物。
2. 产品页：一句话说明它是什么，以及相对已知替代的核心差异；没有替代来源就不做对比。
3. 工作页：只展示理解产品所需的关键步骤，不展示完整内部决策字段。
4. 证明页：优先展示一次真实输入与实际输出；只有规则文档时明确标成“功能合同”，不称为效果证据。
5. 行动页：写准确的安装或查看入口、适用边界和唯一动作。

额外核验：

- 不把当前执行环境误写成产品唯一支持环境。
- GitHub 仓库公开不等于已经提供开源许可证；没有 LICENSE 时不宣传“开源”。
- 未核实成本、订阅或依赖时不宣传“免费”“零成本”。

## 评审已有卡片规划

用户要求评审时，默认只读，不重写。按优先级报告：

1. 事实、证据和来源错误
2. TA 任务与核心价值是否正确
3. 页序是否推动理解、相信和行动
4. 单页标题与上屏文案质量
5. 视觉和交接格式

先给“通过/有条件通过/不通过”，再列证据、影响和最小修正方向。只有用户明确要求修改时才改文件或重写方案。

## 配套小红书笔记

只有用户明确要求笔记，或任务明确包含发布文案时才生成。笔记与卡片使用同一核心价值、证据上限和 CTA，但不强制固定段数、emoji、字数或量化。

不得凭空推荐发布时间、热门标签或效果数字。标签只从对象、内容类型和已确认关键词提取。

## 交付前自检

### 事实与证据

- [ ] 每个场景、替代、疑虑、数字、比较和结果都能定位到来源。
- [ ] 待确认字段没有进入标题、读者变化或上屏文字。
- [ ] 每条主张都区分了原始一手材料、条目摘要、编辑总括、二手转述和独立评测；摘要或转述没有冒充原文。
- [ ] 产品自述只支持功能和边界，没有冒充效果证据。
- [ ] 多主体材料已逐条区分主体、对象、动作、机制层和证据性质。
- [ ] 多主体材料已记录发布状态、可用范围和时间；研究、beta、试用、公司自述部署和个人实测没有混成普遍可用能力。
- [ ] 没有因时间共现或共享词汇，把不同机制写成共同选择、共同原因或行业共识。
- [ ] 研究/招聘/个人建议没有升级为已发布能力、普遍规则或安全效果。
- [ ] 所有“都/共同/一致/同一机制/已经落地/更安全”等集体或效果词都逐词核验；无来源的词已删除或降级。
- [ ] 证据是按主张类型匹配的，并记录范围、时间、口径和不能支持的结论，不使用单一总排序替代判断。
- [ ] 若来源是日报/新闻/快讯，已先标记导语、栏目和条目层级，选择了新闻速览、单条深挖或跨条目判断模式，并写出选材声明。
- [ ] 新闻模式的每个数量、主体数、时间范围和分组都能逐项对回具体条目；没有用同日期、关键词或相邻栏目制造共同命题。
- [ ] 新闻条目页主标题写出主体与具体动作/事件；“依据、为什么、证据性质、应用”等元标签没有代替事件标题。
- [ ] 只看全部主标题时，读者能说出本期发生了什么；若只能说出论证步骤，已重写标题或调整页序。
- [ ] 执行环境、安装入口、许可证和成本表述已核实。

### 策略与页序

- [ ] 新的正式规划已先完成方向确认，或用户已明确要求跳过；确认前没有生成完整方案。
- [ ] `visualize` 的可用性已按当前工具和宿主判断；不可用或失败时已降级为编号选项。
- [ ] 视觉面板的回传已被实际解析；回传失败、超时或重复回传没有被当作确认，且没有重复询问或重复生成。
- [ ] 方向确认的每个选项都说明了适用场景、内容组织、最终交付和关键取舍；默认选项有基于当前材料的推荐理由。
- [ ] 视觉面板、原生控件和编号版使用同一套选项解释；用户只看确认单即可预判各方向会保留、舍弃和交付什么。
- [ ] 核心价值描述的是读者任务或可证明交付，不是标题机制。
- [ ] 封面没有优先介绍内部流程。
- [ ] 每页承担不同职责，页序不是功能目录。
- [ ] 没有可删除的重复页面。

### 标题与上屏文案

- [ ] 标题之间对象称谓、视角和语气协调，但没有被强制成同一形状。
- [ ] 读完全部页面后，封面主标题仍准确概括整组的核心判断、答案或交付。
- [ ] 页面标题准确表达本页新增信息。
- [ ] 每页正文直接解释、展开或证明本页标题，且保留了语义成立所需的信息。
- [ ] 删除标签、图标和分类名后，标题与正文仍能独立传达信息。
- [ ] 标题没有比正文和来源所能支持的结论更强。
- [ ] 内部术语、制作说明和未证实主张没有上屏。

### 制作交接

- [ ] 每页都有最终上屏文字、来源锚点、视觉关系、证据素材和承接/行动。
- [ ] 重点词准确引用上屏原文。
- [ ] 未完成的外部验证、制图或发布没有被描述成已完成。

只有逐项实际核验后才能写“自检通过”。存在待确认、待补证据或未核实事实时，明确列出，不得用勾选代替检查。
